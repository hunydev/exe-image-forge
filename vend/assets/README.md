# Vendored browser assets

The authenticated admin terminal serves these pinned assets locally:

- `@xterm/xterm` 5.5.0
- `@xterm/addon-fit` 0.10.0

The files are copied unchanged from their npm release archives. Keep the
matching license files when updating them, and update both the version list and
the UI asset tests.
